export enum Errors {
  INVALID_REQUEST = 'Invalid request',
  PROBLEM_WITH_STAPLES = 'Problem with staples',
  EMPTY_REQUEST = 'Empty request',
  REQUIRED_QUERY_WORD = 'Required query word',
  LONG_NAME = 'Long name',
  QUERY_ERROR = 'Query error',
  OBJECT_PROBLEM = 'Object problem',
  VARIABLES_PROBLEM = 'Variables problem',
}
